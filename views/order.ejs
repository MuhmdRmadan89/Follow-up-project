<!DOCTYPE html>
<html>
<head>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container mt-5">
<h2>Order <%= order.id %></h2>
<p>Status: <b><%= order.status %></b></p>

<a href="/file/<%= order.file_path %>" class="btn btn-dark mb-3">Download File</a>

<h4>Feedback</h4>
<% feedbacks.forEach(f => { %>
<div class="alert alert-secondary">
<%= f.message %>
<small class="text-muted d-block"><%= f.created_at %></small>
</div>
<% }) %>

<form method="post" action="/admin/approve/<%= order.id %>">
<button class="btn btn-success">Mark Approved</button>
</form>
</body>
</html>